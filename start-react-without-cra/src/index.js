import React from 'react';
import ReactDOM from 'react-dom/client';

const root = ReactDOM.createRoot(document.getElementById("root"));

root.render(
  <React.StrictMode>
    <h1>Just React</h1>
    <p>I Just learned react with install manualy component, that is webpack & babel</p>
  </React.StrictMode>
);